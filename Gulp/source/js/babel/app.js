'use strict';

var test = 'test';
function yolo(e) {
    var height = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 50;
    var color = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 'red';
    var url = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : 'http://azat.co';

    e.preventDefult();
}
var url = 'http://localhost:3000/api/messages/' + id;

var roadPoem = 'Then took the other, as just as fair,\n    And having perhaps the better claim\n    Because it was grassy and wanted wear,\n    Though as for that the passing there\n    Had worn them really about the same,';

var fourAgreements = 'You have the right to be you.\n    You can only be you when you do your best.';

$('.btn').click(function (event) {
    undefined.sendData();
});